<!doctype html>
<html>
  <head>
    <title>{{ page_title }}</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="description" content="{{ page_description | escape }}">
    <link rel="canonical" href="{{ canonical_url }}">
    <meta name="viewport" content="width=device-width,initial-scale=1,shrink-to-fit=no">
    {% comment %} Content_for header is one of the 3 Content Objects in Shopify Liquid {% endcomment %}
    {{ content_for_header }}
    <!-- Header hook for plugins -->
  </head>
  <body>
    <main role="main">
      {% comment %} 
    Content_for_layout is one of the 3 Content Objects in Shopify Liquid
    It is used to render the content of the page.
   This content is the main content of the page, and is the content that is unique to each page.
     {% endcomment %}
      {{ content_for_layout }}
      

      <div class="global-content">
        <div class="product">
          <div class="product__info">
            <h3>{{ all_products['the-trooper'].title }}</h3>
            <p>{{ all_products['the-trooper'].description }}</p>
            <p>{{ all_products['the-trooper'].price | money_with_currency }}</p>
          </div>
        </div>
      </div>
      <hr>
      <div class="control-flow-wrapper">
        <h2>Control flow</h2>
        <h3>If</h3>
        <p>If the product is the trooper, then display a message.</p>
        {% comment %} We use if to check if the product is the trooper. {% endcomment %}
        
        {% if all_products['the-trooper'].title == 'The Trooper' %}
          <p>✅ It's the trooper!</p>
        {% else %}
          <p>⭕️ It's not the trooper!</p>
        {% endif %}

        <h3>Unless</h3>
        {{all_products['the-trooper'].collection }}

        {% comment %} We use unless to check if the product is not the trooper. {% endcomment %}
        <p>Unless the product is a shirt, then display a message.</p>
      {{ all_products['the-trooper'].category }}
        {% unless all_products['the-trooper'].category == 'T-Shirts' %}
          <p> ⭕️ It's not a shirt!</p>
          {% else %}
          <p>✅ It's a shirt"</p>  
        {% endunless %}
      </div>


    </main>
  </body>
</html>
